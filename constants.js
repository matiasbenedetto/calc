const MINUS = '-';
const PLUS = '+';
const MULTIPLY = 'x';
const DIVIDE = '/';

const ERRORS = {
  DIVIDE_BY_ZERO: "You broke math! You can't divide by zero",
  INVALID_EXPRESSION: "Invalid expression",
}

module.exports = {
  MINUS,
  PLUS,
  MULTIPLY,
  DIVIDE,
  ERRORS,
}
